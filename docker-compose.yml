version: '3.8'

services:
  web:
    image: nginx:latest
    container_name: devops-build-app
    ports:
      - "80:80"
    volumes:
      - ./build:/usr/share/nginx/html
    restart: unless-stopped

